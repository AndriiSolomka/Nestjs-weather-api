export enum WeatherTimeframe {
  CURRENT = 'current',
  MINUTELY = 'minutely',
  HOURLY = 'hourly',
  DAILY = 'daily',
  ALERTS = 'alerts',
}

export enum WeatherAttributes {
  SUNRISE = 'sunrise',
  SUNSET = 'sunset',
  TEMP = 'temp',
  FEELS_LIKE = 'feels_like',
  PRESSURE = 'pressure',
  HUMIDITY = 'humidity',
  UVI = 'uvi',
  WIND_SPEED = 'wind_speed',
}
